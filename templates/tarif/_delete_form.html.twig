<form method="post" action="{{ path('app_tarif_delete', {'id': tarif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif.id) }}">
    <button class="btn">Delete</button>
</form>
